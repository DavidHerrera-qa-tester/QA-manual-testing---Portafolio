# Evidence

Use the exact Test Case ID for screenshots:

- `TC-SQL-001.png`
- `TC-SQL-010.png`
- `TC-SQL-011.png`
- `TC-SQL-015.png`
- `TC-SQL-020.png`

A screenshot should show the SQL query and the result grid/result count. For the portfolio, prioritize the more representative cases such as JOIN, integrity validation, GROUP BY/HAVING and LEFT JOIN.
